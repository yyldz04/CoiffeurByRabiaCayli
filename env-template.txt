# Supabase Cloud Configuration
# Copy this to .env.local and fill in your actual values

# Your Supabase project URL (found in Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url_here

# Your Supabase anon/public key (found in Project Settings > API)
# Note: With private tables, this will only work for authenticated users
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Service role key (REQUIRED for edge functions and admin operations)
# This bypasses RLS and should be kept secret
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional: JWT secret for custom auth (if needed)
# SUPABASE_JWT_SECRET=your_jwt_secret_here
